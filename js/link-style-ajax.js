/**
 * @file
 * Defines Javascript behaviors for the Action Link module.
 */

(function ($, Drupal, drupalSettings) {
  'use strict';

  Drupal.behaviors.actionLink = {
    attach: function (context, settings) {
    }
  };
})(jQuery, Drupal, drupalSettings);
